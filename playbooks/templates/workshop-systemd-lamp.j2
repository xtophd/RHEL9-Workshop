#!/bin/bash

echo ""
echo "Installing packages for lamp stack"
dnf install -y httpd mariadb-server mariadb

echo ""
echo "Enabling services"
systemctl enable --now httpd mariadb

echo ""
echo "Enabling firewall"
firewall-cmd --add-service=http

