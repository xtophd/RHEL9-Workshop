#!/bin/bash

##
##    This script kills the current tty session, there-by
##    returning a participant to the bastion-host (or 
##    whereever they connected from.
##

[[ $(hostname -s) != "bastion" ]] && pkill -9 -t $(tty | sed -e 's|/dev/||')
